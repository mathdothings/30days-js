const fullStack = [
  ["HTML", "CSS", "JS", "React"],
  ["Node", "Express", "MongoDB"],
];

for (let array of fullStack) {
  for (let element of array) {
    console.log(element.toUpperCase());
  }
}
